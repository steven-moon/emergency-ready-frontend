export default {
	state: () => ({
		/**
		 * Notifications for Company App
		 *
		 * @type {Array}
		 */
		notifications: [],

		/**
		 * Single Notification
		 *
		 * @type {Array}
		 */
		notification: null,

		/**
		 * Notification Types for Company App
		 *
		 * @type {Array}
		 */
		notificationTypes: [],
	}),

	// Add other properties like getters, actions, and mutations here if needed
};
